{
  "name": "protfolio-a26",
  "private": true,
  "description": "Wrapper scripts so the app can also be run from the repository root.",
  "scripts": {
    "postinstall": "npm --prefix shihab-portfolio install",
    "dev": "npm --prefix shihab-portfolio run dev",
    "build": "npm --prefix shihab-portfolio run build",
    "preview": "npm --prefix shihab-portfolio run preview",
    "deploy": "npm --prefix shihab-portfolio run deploy",
    "set-admin-password": "node scripts/set-admin-password.mjs"
  }
}
