Drop two files in this folder and the portfolio picks them up automatically:

1. profile.jpg  — your professional photograph (square or 4:5 portrait works best).
                  Until it exists the hero shows an initials avatar instead.

2. MD-Shihab-Hossain-Resume.pdf — the PDF served by every "Download resume" button.

Both paths are editable from the admin panel (Profile → Contact & files), so you can
rename them or point at an external URL instead.
